version: 2.1
jobs:   
  build-package:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
          name: Build
          command: |
            cd Micro_Service_Postgres/contact_service; 
            mvn clean package;          
      - store_artifacts:
          path: contact_service/target/*.jar
          destination: artifact-file

 
                 


workflows:
  lincker: 
    jobs:
      - build-package
