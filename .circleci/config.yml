version: 2.1
jobs:   
  build-package-contact_service:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
          name: Build
          command: |
            cd Micro_Service_Postgres/contact_service; 
            mvn clean package;
            ls;
            ls -R | grep .jar;
            pwd;
  build-package-user_service:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - checkout
      - run:
          name: Build
          command: |
            cd Micro_Service_Postgres/user_service; 
            mvn clean package;  
            ls;
            ls -R | grep .jar;
            pwd;        
                 


workflows:
  lincker: 
    jobs:
      - build-package-contact_service
      - build-package-user_service
